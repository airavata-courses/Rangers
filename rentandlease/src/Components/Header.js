import React, { PureComponent } from "react";
import "./Header.css";

export class Header extends PureComponent {
  render() {
    return (
      <div id="header">
        This is the Header
        <hr />
      </div>
    );
  }
}

export default Header;
